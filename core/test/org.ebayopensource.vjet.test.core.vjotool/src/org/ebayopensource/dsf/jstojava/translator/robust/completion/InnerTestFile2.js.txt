vjo.ctype('com.ebay.test.validation.Test') //< public
.protos({
        doIt : vjo.ctype()
        .protos({
            d:vjo.NEEDS_IMPL,
            innerProp :10,//<String
            //>public void test() 
            test : function(){
                  this.innerProp.
            }
        })

})
.endType();
