vjo.itype('syntax.declare.itype.ITypeExample3')
.expects()
.props({
	//> public int
	initialValue: undefined

})
.protos ({

})
.inits(
	function(){
		/* use this section for static initialization */
		this.initialValue = 100;
	}
)
.endType();
