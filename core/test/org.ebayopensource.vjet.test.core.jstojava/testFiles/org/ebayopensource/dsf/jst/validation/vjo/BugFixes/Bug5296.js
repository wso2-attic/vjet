vjo.ctype("org.ebayopensource.dsf.jst.validation.vjo.BugFixes.Bug5296")
.props({
		oCache: {},
        add: function(pJson) {
	        try {
	            var t = this, kw = pJson[0], kwList = pJson[1], laList = pJson[2], cacheItem = t.oCache[kw];
	            // process lookahead data
	            if (laList !== null) {
	                for (var i=0;i < laList.length; i++) {
	                    
	                }
	            }
	        } 
	        catch(e) {
	        }
	    }
})
.endType();
