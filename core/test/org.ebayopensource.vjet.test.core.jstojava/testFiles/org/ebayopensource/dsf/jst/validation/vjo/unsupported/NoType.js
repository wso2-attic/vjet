vjo.ctype('org.ebayopensource.dsf.jst.validation.vjo.unsupported.NoType')//<public
.protos({
	 foo : function(a ,b){
                var c = a*b;
                c = a-b;
                c = a+b;
                
                if(Math.abs(a) - b > 0.001){
                	
                }
                
                b.length;
		        b.toLowerCase();
		        b.toUpperCase();
                
                var abc;
		        abc.length;
		        abc.toLowerCase();
		        abc.toUpperCase();
     }
})
.endType();