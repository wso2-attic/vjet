vjo.ctype('vjoPro.samples.server.ServerEx11') //< public
.inherits('vjoPro.samples.server.ServerEx7')
.protos({
/**
* @access public
* @param {int} psId
*/
//> public constructs(int psId)
constructs : function (psId) {
this.base(psId);
}
})
.endType();
