vjo.ctype("vjoPro.dsf.utils.logging.Formatter")
.protos({
constructs: function(name){//<public constructs(String)
this.name = name; // help to debug
},

format: function(lr){
return '';
}
})
.endType();
